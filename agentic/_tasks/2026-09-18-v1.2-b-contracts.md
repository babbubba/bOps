# V1.2-B — delegation contracts in `bOps.Abstractions`

Status: **next**
Recommended model effort: **molto alto**
Repository: `bOps`
Depends on: V1.2-A complete (ADR-0030 Accepted)
Governing ADR: `docs/architecture/adr/0030-privilege-reducing-multi-agent-delegation.md`

## Outcome

Add the product-neutral, dependency-free public contracts (ADR-0030 sections 1, 3, 4, 7, 8, 10) and bump the SDK to `1.2.0-preview.1`.

## Checklist

- [ ] Write failing round-trip tests first for every new type (rule A2, source-generated JSON).
- [ ] `AgentIdentity`, `AgentRoleKind`, `AuthorityEnvelope` and its reduction result, budgets, `DelegationRun`, journal entries, `VerificationReport`, `IDelegationStore`, new audit event types and the optional `Delegation` correlation block.
- [ ] Optional provenance on `Evidence`; nothing in the frozen 1.0 surface removed or changed (check with the API diff used in V1.1-H).
- [ ] No dependency added to `bOps.Abstractions`; XML documentation on every public member.
- [ ] Architecture tests (A1, A7) stay green; the SDK packs as `1.2.0-preview.1`.

## Definition of Done

- [ ] Contracts are additive, documented and round-trip tested.
- [ ] Release build and the non-live suite are green on Windows and Linux CI.
