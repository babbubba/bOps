# V1.2-L — documentation and alignment

Status: **implemented** (2026-09-21; CI green on pull request #22)
Recommended model effort: **basso**
Repository: `bOps`
Depends on: V1.2-K complete
Governing ADR: `docs/architecture/adr/0030-privilege-reducing-multi-agent-delegation.md`

## Outcome

Document the model and its explicit limits, and align the tracking documents.

## Checklist

- [x] Document the delegation model, roles, authority reduction, approval and resume behavior.
- [x] Extend `docs/security/threat-model.md` with the limits of the envelope, stated where V1.2-C2 found them: targets and environments are matched only for a step that carries a Skill scope, never for a plain Read call, whose reach stays bound by its own argument validation and path policy (ADR-0031 §4, S2, S11); an envelope restricts calls that go through the runtime's delegated entry points, and does nothing against code that does not (S8); a role that used a public, non-delegated entry point would run unrestricted, so the orchestrator is the trust boundary (V1.2-D); the post-action verification of an executed step runs outside the envelope, named by the tool's own manifest (rule S4); the envelope refusal is audited as a Forbidden policy decision (interpretation 3 of C2).
- [x] Document the `delegation` section of `policy.yaml` for operators, from the schema recorded in `2026-09-18-v1.2-c-authority-reduction.md` ("The `delegation` section"): what an absent key grants, that `maxRisk`, `maxBlastRadius` and `maxDuration` are required, that unknown keys are errors, that a window instant needs a zone, that `maxDuration` is `hh:mm:ss`, which dimensions each role may not be given, and that a malformed section makes the whole policy `AllForbidden` (interpretation 1 of C3), with a sample `policy.yaml`.
- [x] State the explicit absence of parallelism, agent approval, microservices and remote guarantees.
- [x] Align README, CHANGELOG, HANDOFF, roadmap and task index with the validated results.

## Definition of Done

- [x] Documentation matches what was verified, with no forward-looking claims.

## Evidence for L

| Check | Result |
|---|---|
| Model | `docs/agents/delegation.md`: the pipeline and roles, authority reduction per dimension, separation of duties, budgets, durability, resume and reconciliation, audit, surfaces. Each statement was checked against the code or a task's recorded evidence, not written from the ADR alone (for example, that all four role profiles are needed to derive a root envelope, read from `EnvelopeReducer.DeriveRootAttributed`). |
| Envelope limits | Threat model, "Limits of the authority envelope": targets and environments matched only for a step with a Skill scope; an envelope binds the delegated entry points and not code that bypasses them (S8), so the orchestrator is the trust boundary; post-action verification outside the envelope (S4); a refusal audited as a `Forbidden` policy decision; budgets and deadline enforced by the orchestrator; the tool view narrowed as a courtesy; a malformed section failing the policy to `AllForbidden`. |
| Operator guide | `docs/agents/delegation-policy.md`: what an absent key grants, the required keys, exact names, `hh:mm:ss`, window zones, dimensions a role may not be given, unknown keys, `AllForbidden`, with a sample. `DelegationPolicyGuideTests` loads the sample from the document and checks that the rules stated are the ones the loader enforces (a required key left out, an unknown key, a bare duration, a window without a zone, a numeric risk, Skills given to Verification). |
| Absences | Stated in `delegation.md`: no parallelism, no agent approval, no agent conversation or model-chosen roles, no microservices or separate processes, no remote or multi-node guarantees, no per-role model, no plugin roles, not a sandbox. |
| Alignment | README (roadmap, status, usage, documentation table), CHANGELOG, HANDOFF, consolidated roadmap, bootstrap, the V1.2 task and the task index all say A–L implemented and M open; nothing says V1.2 is released. |
| Tests | `bOps.Policy.Tests` 235 (2 new). No other code changed. |
| Full suite | CI on pull request #22 (run 35513758481): ubuntu-latest, windows-latest and dependency-review all passed. |

## Interpretations made in L

None changes an ADR.

1. **Known limits are in the model page.** The runtime not writing `AwaitingApproval`, the English-only dashboard and the CLI not enforcing the
   administrator role are stated in `delegation.md` rather than hidden, because they are what an operator would otherwise discover.
2. **The guide's example is a test fixture.** It is a real section, loaded by a test, so a change to the loader that breaks it fails the build.
3. **The README status stays at V1.1.** The V1.1 paragraph is unchanged; a separate paragraph says V1.2 is in progress and not released.
