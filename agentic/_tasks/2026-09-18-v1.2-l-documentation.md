# V1.2-L — documentation and alignment

Status: **planned**
Recommended model effort: **basso**
Repository: `bOps`
Depends on: V1.2-K complete
Governing ADR: `docs/architecture/adr/0030-privilege-reducing-multi-agent-delegation.md`

## Outcome

Document the model and its explicit limits, and align the tracking documents.

## Checklist

- [ ] Document the delegation model, roles, authority reduction, approval and resume behavior.
- [ ] Extend `docs/security/threat-model.md` with the limits of the envelope, stated where V1.2-C2 found them: targets and environments are matched only for a step that carries a Skill scope, never for a plain Read call, whose reach stays bound by its own argument validation and path policy (ADR-0031 §4, S2, S11); an envelope restricts calls that go through the runtime's delegated entry points, and does nothing against code that does not (S8); a role that used a public, non-delegated entry point would run unrestricted, so the orchestrator is the trust boundary (V1.2-D); the post-action verification of an executed step runs outside the envelope, named by the tool's own manifest (rule S4); the envelope refusal is audited as a Forbidden policy decision (interpretation 3 of C2).
- [ ] Document the `delegation` section of `policy.yaml` for operators, from the schema recorded in `2026-09-18-v1.2-c-authority-reduction.md` ("The `delegation` section"): what an absent key grants, that `maxRisk`, `maxBlastRadius` and `maxDuration` are required, that unknown keys are errors, that a window instant needs a zone, that `maxDuration` is `hh:mm:ss`, which dimensions each role may not be given, and that a malformed section makes the whole policy `AllForbidden` (interpretation 1 of C3), with a sample `policy.yaml`.
- [ ] State the explicit absence of parallelism, agent approval, microservices and remote guarantees.
- [ ] Align README, CHANGELOG, HANDOFF, roadmap and task index with the validated results.

## Definition of Done

- [ ] Documentation matches what was verified, with no forward-looking claims.
