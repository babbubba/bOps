# V1.3-M8 — release, security and acceptance gate record

Parent packets: `2026-09-22-v1.3-m-execution-packets.md` (M8), `2026-09-16-v1.3-oss-entitlement-plugin-lifecycle.md`
Branch: `feat/v13-m1-entitlement-adr` · Gate started at `df63b30` · Host: Windows 11 Pro 10.0.22631, Docker Desktop (Linux engine)

**Outcome: V1.3-M8 LOCAL GATE PASS — EXTERNAL/CI EVIDENCE PENDING.**
Every criterion that can be executed on this host passes. Linux real-host tests, elevation- and symlink-privilege-gated Windows
tests, and Windows/Linux CI on the branch have not run and are recorded `PENDING`, never `PASS`. No push, PR, merge, tag or
publication was made, and none was authorized.

A green build is not release evidence by itself. `PASS` below means a named test or a named mechanical scan discriminates the
criterion; where the evidence is structural rather than behavioural it says so.

## Commands run

| Gate | Command | Result |
|---|---|---|
| .NET build | `dotnet build bOps.slnx --configuration Release` | 0 warnings, 0 errors (repeated after the M8 test additions) |
| .NET tests | `dotnet test bOps.slnx --no-build -c Release --filter "Category!=LiveModel"` (one complete run) | 2,730 tests: 2,623 passed, 105 skipped, **2 failed** (both load-sensitive, see F-1/F-2) |
| Affected projects, quiet host | Api.Tests alone; Docker.Tests alone; Architecture, Runtime, PluginHost, Audit, Cli alone | Api 160/160; Docker 259 passed, 9 skipped, 0 failed; Architecture 68/68; Runtime 897/897; PluginHost 125/125; Audit 11/11; Cli 84/84 |
| Angular | `npm ci`, `npm run build`, `npm run lint:i18n`, `ng test --watch=false --browsers=ChromeHeadless` | install: 0 vulnerabilities; build OK; i18n self-test 22 cases OK, 10 templates clean; tests **172/172**, 0 skipped |
| Quality framework | `Test-MilestoneQuality.ps1 -TestSelf` | `SELF PASS`. No V1.3-M manifest exists (only G and H); the frozen framework was not extended. Any score is telemetry, not certification. |

The two full-run failures were investigated once each, as the flake rule requires:

- **F-1** `Api.Tests.AgentsAndApprovalsEndpointsTests.ApprovalFlow_BlocksThenExecutes_WhenAnOperatorRespondsThroughARequest` —
  `TaskCanceledException` in `PollUntilAsync` after 21 s under full-suite load. It is the registered
  `pre-existing-environment-timing-sensitive` entry in `known-failures.json`. Passes in isolation and inside the complete Api
  project run alone (160/160). Not weakened, no sleep changed.
- **F-2** `Docker.Tests.DockerRunnerFlowTests.AVolumeIsCreatedAndRemovedThroughTheRunner_EachVerified_AndOnlyTheRemovalAsksForAHuman` —
  "The Docker daemon could not be reached: The operation has timed out" while the real Docker Desktop daemon was contended by
  the concurrent Windows test projects. Passes in isolation and inside the complete Docker project run alone. The product
  behaved correctly: it returned a failed, refuted result rather than an unverified success. This test is **not** in
  `known-failures.json`.

Neither test is treated as a deterministic pass: both are recorded as load-sensitive, and Windows/Linux CI must confirm them.

## Skip audit (105 skipped in the full run)

| Count | Reason | Classification |
|---|---|---|
| 83 | Linux-only (System.Linux 61, Network.Native.Linux 11, Service.Linux systemd 3, Storage.Linux 2, Linux-only source tests 2, Filesystem Linux permission model 4) | PENDING required platform evidence — needs Linux CI |
| 6 | Real Windows service lifecycle needs an elevated process | PENDING required platform evidence — needs an elevated Windows runner |
| 1 | Real Windows Task Scheduler needs registration permission (`BOPS_RUN_REAL_SCHEDULER_TESTS=1`) | PENDING required platform evidence |
| 15 | Symbolic-link creation needs a privilege this process lacks (Filesystem 6, Docker build-context 9) | PENDING required platform evidence — Windows CI or Developer Mode |

No skip is unexpected. The Windows real-host tests that the V1.3 areas require did execute and pass: identity (13), firewall
(15), System.Windows (107, covering updates 11, crash evidence 8+1, driver evidence 4, events 32, process 20+9, tools 23),
network native (4 + 2 loopback sockets), storage (3), scheduler deterministic (8) and service (9). TLS/certificate coverage runs in
`Packages.Security.Tests` (7/7) and `TlsConnectionScenarioTests`.

## Acceptance evidence

`PASS` = discriminating evidence exists and passed · `PENDING` = required evidence cannot execute here · no `FAIL`.

| ID | Criterion | Evidence | Status |
|---|---|---|---|
| M8-01 | Release build is warning-free | Build output above | PASS |
| M8-02 | Full non-live .NET suite | One complete run plus quiet reruns; F-1/F-2 are load-sensitive and pass alone | PASS (qualified by F-1/F-2; CI must confirm) |
| M8-03 | Angular clean install, build, i18n lint, headless tests | 172/172 | PASS |
| M8-04 | Architecture suite (incl. no generic execution tool, core names no package, frozen public surface, additive 1.3.0-preview.1) | 68/68 | PASS |
| M8-05 | Audit contracts: discriminators, chain, neutral vocabulary | `Audit.Tests` 11/11; `EntitlementContractTests` 11/11; lifecycle audit round-trip | PASS |
| M8-06 | Windows real-host packages for the V1.3 areas | See skip audit; no Windows-required test skipped except the gated ones below | PASS |
| M8-07 | Windows elevation-, scheduler-provisioning- and symlink-privilege-gated tests | 22 skipped | PENDING (required) |
| M8-08 | Linux real-host tests | 83 skipped on this host | PENDING (required) |
| M8-09 | Windows/Linux CI on this branch | Not run; push not authorized | PENDING (required) |
| M8-10 | Standalone OSS unaffected when entitlement does not apply | `UngovernedWithoutService_ExecutesNormally`; `ToolRegistryTests` default `NotGoverned` | PASS |
| M8-11 | Applicability is host-controlled, not model- or provider-controlled | `Applicability_IsHostRequirementAndNotProviderRequestOrDecision`; registry copy-immutability tests | PASS |
| M8-12 | Governed operation fails closed: no service, provider exception, wrong binding, future, expired, missing window, inverted window, missing authority, undefined result | `GovernedMissingService`, `GovernedProviderFailure`, `GovernedAllowWithWrongBinding`, `GovernedUnusableDecision_FailsClosedWithoutExecuting` (7 cases), in-flight expiry tests. Mutation-checked: removing the not-yet-valid guard fails the future cases | PASS |
| M8-13 | Entitlement is checked after policy and approval and immediately before the side effect | `GovernedApprovalThenRevocation`, `GovernedDeniedAfterApproval`, `PolicyForbidden_NeverConsultsEntitlement`, `ApprovalRejected_NeverConsultsEntitlement`, `GovernedAllow_ExecutesOnce` | PASS |
| M8-14 | Governed verifier invocation is entitled; unauthorized verification is `Inconclusive` and skips the evaluator | `GovernedVerificationDenied`, `GovernedVerificationAllowed`, `GovernedVerificationWithoutAUsableDecision` (provider failure, wrong binding, expired, future), `VerifyPlanAsync_EntitlementDeniedRead_IsInconclusive` | PASS |
| M8-15 | Retry, replan, repeated denial and delegated plan steps cannot bypass entitlement | `GovernedRetry_UsesAFreshBinding`, `GovernedModelCallDenial_EntersTheExistingReplanPath`, `RepeatedGovernedDenial_StopsAtConfiguredConsecutiveDenialGuard`, `ExecuteDelegatedPlanAsync_EntitlementDenialStopsBeforeDependentStep` | PASS |
| M8-16 | Request binding rejects stale or mismatched decisions | wrong-binding tests above; per-attempt binding asserted `NotEqual` | PASS |
| M8-17 | Audit is neutral and carries no token material or unredacted arguments | `GovernedDeniedCall_RedactsSensitiveArgumentsBeforeAudit`; contract tests reject raw/commercial properties | PASS |
| M8-18 | No commercial tier, price, SKU, payment provider, private repository reference, token/licence format, private key, commercial playbook or Coordinator/Portal implementation in the public trees | New `OssLeakScanTests` (15): tree scan plus per-rule positive controls and neutral-vocabulary negatives; a targeted pre-scan found no hits | PASS |
| M8-19 | Provider-owned rows: per-dimension mismatch (subject/installation/feature/Skill/version/capability/node/target), resource exhaustion, provider cache/offline, provider-side clock rollback | The public runtime decides only on binding and validity window; these belong to the signed provider in `bOps.Commercial` (ADR-0036) | PENDING — not required by the public gate; private companion |
| M8-20 | Archive is untrusted and bounded: traversal, rooted, reserved names, duplicates, link/device entries, entry-count, per-entry, total and compressed bytes, ratio bomb, depth, path length, hard ceilings; nothing retained on rejection | `PluginArchiveStagerTests` 5 → 18. Mutation-checked: weakening the ratio guard fails the bomb test | PASS |
| M8-21 | Validation never executes the candidate | Structural: `PluginCandidateInspector` uses `PEReader` only; `PluginLifecycleBoundaryTests` proves assemblies load only in `PluginManager`/`PluginLoadContext`, reached only from the lifecycle service; `InvalidCandidates_…` rejects a non-managed assembly without loading | PASS (structural, not a runtime execution sentinel) |
| M8-22 | Single trust and loading authority | Same tests; signature/trust categories in `InvalidCandidates_…` | PASS |
| M8-23 | Generation roles A/B/C, journal, crash recovery, LKG digest binding, uncommitted candidate fail-closed, legacy migration | `PluginLifecycleServiceTests` (56), incl. interruption theories at every commit point, `Recover_Refuses…DigestIsNotTheRecordedLkgGeneration`, `RecoveryRequired…UncommittedCandidate…` | PASS |
| M8-24 | ETag, idempotency, per-plugin serialization | `Revision_Changes…`, `IdempotencyScope_IsNodeActorKey`, `SameKey…Conflicts`, `SamePlugin_MutationsAreSerialized`, restart replay | PASS |
| M8-25 | Explicit exact-version activation; partial-activation cleanup; startup `ActivationFailed`; cleanup observability | `Enable_RequiresExplicitConfirmation…`, `ActivationFailure_AfterPartialRegistration…`, `StartupActivationFailure_IsPersistedAsActivationFailed…`, `CleanupFailure_IsAudited…` | PASS |
| M8-26 | Install never auto-enables; Recover never enables; no Delete lifecycle path | `FirstInstall_CommitsDisabled_NeverEnables`, `ReplacementInstall_NeverAutoExecutes…`, `Recover_Restores…AsDisabled…ExecutesNothing`, API `Status_OfAnUnknownPlugin_Is404_AndThereIsNoDeleteOperation`, boundary test (no `MapDelete`, CLI `remove` only refuses) | PASS |
| M8-27 | No first-party bypass of the lifecycle service | Static search plus permanent `PluginLifecycleBoundaryTests` (12). Mutation-checked: injecting `manager.Disable` in the CLI fails it. Result: NONE | PASS |
| M8-28 | Lifecycle audit is neutral (no bytes, paths, keys, idempotency key, stacks, actor/node from the request) | `Audit_RecordsLifecycleOutcomes_WithoutPathsPayloadsOrStacks`, `CleanupFailure_IsAudited…`, API audit assertions | PASS |
| M8-29 | API authorization: administrator only, anonymous/non-admin zero effect, bearer only, forged role claims and cookies rejected | `PluginLifecycleEndpointsTests` (47), incl. each single non-admin role | PASS |
| M8-30 | API transport: actual-byte body bound, `application/zip` only, trusted actor/node, `If-Match`/`If-None-Match`, `Idempotency-Key`, concurrency, sanitized errors, `lifecycleFailure`, `recoveryAvailable` | Same class plus `PluginLifecycleCompositionTests` (3); complete Api project 160/160 alone | PASS |
| M8-31 | UI: admin-only controls, no delete, lifecycle status/failure/recoveryAvailable, trust/risk/compatibility, unknown state fail-closed | `plugins.spec.ts`, `plugin-lifecycle.spec.ts` (Angular 172/172) | PASS |
| M8-32 | UI: `application/zip`, `If-None-Match`, opaque `If-Match`, idempotency key, double-submit, exact-version confirmation with the not-sandboxed warning, 412/428/409 never auto-retried, Recover never enables | Same specs | PASS |
| M8-33 | Broader safety axiom: no generic shell/process-start/SQL/env dump; runtime, policy and audit stay product-neutral | `NoGenericExecutionToolTests`, `CoreNamesNoPackageTests`, `V13DiagnosticSurfaceTests`; no new tool surface in M1–M8 | PASS |
| M8-34 | Public API compatibility and version evidence | `PublicSurfaceSnapshotTests`; `AbstractionsVersion_AdvancesAdditivelyForV13Contracts` (`1.3.0-preview.1`, additive only). No version change made by M8 | PASS |

**Totals: 34 criteria — PASS 30, FAIL 0, PENDING 4 (M8-07, M8-08, M8-09 required; M8-19 not required, private companion).**

## Findings

| Severity | Finding |
|---|---|
| BLOCKER | none |
| HIGH | none |
| MEDIUM | **F-2** — a real-Docker-daemon flow test timed out once under full-suite load and is not in the known-failures registry; product behaviour was correct. Decide whether to register it or harden its daemon wait; do not raise arbitrary sleeps. |
| MEDIUM | **Evidence gaps closed by M8** (not defects): no public-tree leak scan existed; ADR-0036 matrix rows for future/malformed validity, policy/approval-before-entitlement ordering and verification-side provider failure or wrong binding lacked a dedicated test; the parent task's symlink, bomb and excess files/bytes upload attacks and a permanent lifecycle-bypass guard were untested. Tests only; no production code changed. |
| LOW | `PluginManager.Install/Enable/Disable/Remove/LoadAllEnabled` remain public. Nothing first-party reaches them (guarded now); a later internalization would shrink the surface but would change a public member and is not an M8 fix. |

Unresolved operator decisions: none.

## Pending external evidence (needed before V1.3 is called released)

1. Windows and Linux CI green on this branch after an authorized push (M8-07, M8-08, M8-09), including F-1/F-2.
2. A Linux run of the 83 Linux-gated tests; an elevated Windows run of the 6 service tests and the real Task Scheduler test; symlink privilege for the 15 link tests.
3. Sol Low security review of the packet's scope (entitlement bypass, stale approval race, audit leakage, archive/trust bypass, LKG failure, authorization/idempotency, dependency direction), if the operator still wants it as a separate step.
