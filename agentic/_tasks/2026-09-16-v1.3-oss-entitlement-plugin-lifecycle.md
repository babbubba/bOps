# V1.3-M public track — entitlement boundary and plugin lifecycle UI

Status: **future; do not start before V1.3-L and repository-topology gates close**
Recommended model effort: **molto alto**
Repository: `bOps`
Depends on: V1.3-L; generic entitlement ADR; repository-topology gate before private consumption

## Outcome

Add a product-neutral, fail-closed entitlement hook with resource/feature/node limits and safe local
UI/API operations to upload, enable and disable trusted plugins without placing Community or
commercial business logic in the public repository. Standalone OSS operation remains independent of
account registration and official-distribution entitlement.

## Entitlement ADR and implementation

- [ ] Define `IEntitlementService` and neutral request/decision contracts for subject,
      installation, feature, Skill/version, capability, resource/limit, node/target, validity,
      constraints and reason code. Do not introduce `ISkillEntitlementService` first.
- [ ] Keep product tiers, Community/commercial names, prices, SKU, payment providers and vendor
      token formats outside the public contract; consume concrete grants and limits only.
- [ ] Place enforcement after approval validation and immediately before execution so stale or
      revoked entitlement cannot execute.
- [ ] Define offline/cache/clock-skew semantics; absence, invalidity and provider failure fail closed.
- [ ] Keep vendor token/license contents out of audit/log/telemetry and define the generic state
      transition seen by already-running work when entitlement changes.
- [ ] Audit source category and decision without token/license contents.
- [ ] Keep read-only inspection available when execution entitlement is denied.
- [ ] Preserve unrelated standalone OSS capabilities when an official-distribution entitlement is
      absent, invalid or expired.
- [ ] Add architecture scans rejecting commercial names, prices, payment providers, private license
      identifiers, prompts, fixtures and playbooks in the OSS tree.

## Plugin lifecycle ADR and API

- [ ] Define administrator-only enable/disable/upload state transitions, idempotency, concurrency,
      recovery and audit.
- [ ] Define strict upload byte/file-count limits, accepted archive shape, traversal defense,
      quarantine/staging location and cleanup.
- [ ] Validate manifest schema, host/SDK compatibility, capabilities, dependencies, detached
      signature, publisher trust and risk ceiling before installation.
- [ ] Install atomically; a failed upload or activation never replaces the last known-good plugin.
- [ ] Reuse PluginManager/PluginHost; do not create a second loader or trust policy.
- [ ] Preserve the explicit warning that enabled in-process code has host privileges.

## Angular work

- [ ] Extend the V1.1-F catalog with role-gated enable/disable/upload controls.
- [ ] Show validation stages, publisher/signature/trust, compatibility and requested capabilities
      before final activation.
- [ ] Require explicit confirmation for activation and display rollback/recovery outcome.
- [ ] Prevent double submission and stale-state mutation with version/ETag handling.

## Validation

- [ ] Entitlement tests: valid, expired, future, revoked, wrong subject/installation/feature/Skill/
      version/capability/node/target, resource-limit exhaustion, missing provider, cache/offline,
      clock rollback and post-approval revocation.
- [ ] Upload attack tests: zip slip, symlink, bomb, excess files/bytes, malformed manifest, unknown
      key, bad signature, incompatible SDK, dependency failure and crash mid-install.
- [ ] Authorization/CSRF/idempotency/concurrency tests for every lifecycle endpoint.
- [ ] UI tests for allowed/denied roles and recovery states.
- [ ] Release build, full suites, cross-platform CI and OSS leak scan.
- [ ] Coordinate public contract version with private provider consumption without copying private
      implementation into public tests.

## Definition of Done

- [ ] Entitlement is neutral, fail-closed and enforced at the execution point.
- [ ] Plugin lifecycle mutations preserve trust, last-known-good state and complete audit.
- [ ] No commercial implementation detail is present in `bOps`.

## Private companion routing (not executable here)

After the public V1.3-M contract is implemented and all private legal/security/governance gates close, `bOps.Commercial` creates
separate tasks for the signed entitlement provider/offline format and for Account/Coordinator
activation, transfer and recovery. Those tasks own 90-day Community validity, 30-day grace,
one-active-Coordinator enforcement and commercial air-gapped import/export. They must not be
created or implemented merely because this public task has been clarified.

## Coding-agent execution packets

Implement M only through M1–M8 in
[`2026-09-22-v1.3-m-execution-packets.md`](2026-09-22-v1.3-m-execution-packets.md), following
[`../_plans/2026-09-22-v1.3-coding-agent-execution.md`](../_plans/2026-09-22-v1.3-coding-agent-execution.md).
M1 (entitlement ADR) and M4 (plugin lifecycle ADR) are architecture-only gates and must be accepted
before their implementation packets start. A coding agent must never attempt all of V1.3-M in one
session.
