# V1.3-F — filesystem troubleshooting helpers

Status: **complete and merged**
Recommended model effort: **medio**
Repository: `bOps`
Depends on: V1.3-E complete; Filesystem path policy/S11

## Outcome
Add safe log/content/permissions/locking diagnostics plus single-file copy and mkdir, always through existing deny-by-default FilesystemPathPolicy.

## Tools
### fs.grep — Read
Parameters: path, pattern, recursive=false, regex=false, caseSensitive=true, maxMatches default 200/max 2000, maxOutputBytes default 32768/max 131072.
Stream line-by-line; do not follow symlinks/reparse points. Regex uses compiled .NET Regex with per-match timeout (default 250 ms, max 1 s). Return file,lineNumber,boundedLine; cap one line to 2048 UTF-8 bytes. Skip probable binary files via small bounded heuristic.

### fs.tail — Read
Parameters path, lines default 100/max 5000, maxBytes default 65536/max 1 MiB.
Read backwards from a regular file; no follow/stream. UTF-8/BOM support; unsupported/binary encoding reported explicitly.

### fs.permissions — Read
Parameter path. JSON: `path,exists,type,owner,group,unixMode,aclEntries,readOnly,source,complete`.
Windows uses System.Security.AccessControl; Linux uses File.GetUnixFileMode + stat owner/group resolution. Max 200 ACL rows. Do not claim effective authorization for arbitrary users.

### fs.locks — Read
Parameter path; limit default 100/max 1000.
Rows `pid,processName,user,accessKind,source`.
Windows uses Restart Manager APIs for the exact canonical file. Linux scans visible `/proc/<pid>/fd` and optionally correlates `/proc/locks`, with hard process/FD ceilings. Empty result + complete:false when visibility was restricted.

### fs.copy — Medium
Single regular file only. Parameters source,destination. Source read-allowed, destination write-allowed, both re-resolved immediately before operation. Destination must not exist. Config `Filesystem:Operations:MaxCopyBytes` default 1 GiB/hard max 10 GiB.
Add Read `fs.copy.verify` accepting source+destination and comparing existence,length,SHA-256. Copy via temporary sibling then rename when supported; clean only temp created by this invocation.

### fs.mkdir — Low
Parameter path. Write policy required. Idempotent if directory already exists; fail if a non-directory exists. Verify via fs.stat.

## Validation
S11 traversal/symlink/TOCTOU tests; grep regex timeout/binary/huge-line; tail CRLF/LF/BOM/large file; real Windows ACL/Linux mode; real lock holder; copy no-overwrite/limit/temp-cleanup/hash verification/cancellation; mkdir idempotency.

## Definition of Done
The agent can search/tail logs, inspect ownership/ACLs, identify file holders and safely stage a file/create a directory without weakening path policy.

## Execution checklist

### Analysis
- [x] Confirm V1.3-E is merged and read Filesystem path-policy/security constraints.
- [x] Create the implementation plan and identify the existing cross-platform Filesystem package boundary.

### Implementation
- [x] Add bounded read diagnostics and platform-specific permissions/lock evidence.
- [x] Add verified governed copy and mkdir operations.
- [x] Register options and tools in both hosts.

### Tests
- [x] Add limits, encoding, binary, timeout, path-policy and verification coverage.
- [x] Add real platform-gated permissions and lock evidence.

### Documentation and validation
- [x] Align operator documentation, README, CHANGELOG, roadmap and HANDOFF.
- [x] Build and run the non-live suite locally. Push, Windows/Linux CI and merge remain open —
      they require explicit operator authorization (not performed in this pass).

## Local validation evidence (2026-09-22)

- `dotnet build bOps.slnx --configuration Release` — 0 warnings, 0 errors.
- `dotnet test bOps.slnx --configuration Release --no-build --filter "Category!=LiveModel"` —
  2,159 passed, 102 skipped, 3 failed (2,264 total). All three failures are pre-existing,
  environment/timing-sensitive tests unrelated to this task's changes, confirmed by rerunning each
  in isolation: `FsSizeTests.FsSize_StreamsTenThousandFiles_WithBoundedResponse` and
  `FsDeleteTreeTests.PrepareAndPage_TenThousandFiles_WithoutReturningOneUnboundedList` both exceed
  their internal duration budget only under full-solution parallel contention and pass cleanly
  alone (35 s and 1 m 11 s respectively, against ~120 s budgets); neither test nor the code path it
  exercises (`FilesystemDeletionService`, `FsSizeTool`) was touched by V1.3-F.
  `AgentsAndApprovalsEndpointsTests.ApprovalFlow_BlocksThenExecutes_WhenAnOperatorRespondsThroughARequest`
  is an HTTP polling timeout under the same load, unrelated to the Filesystem package.
- `dotnet test tests/bOps.Packages.Filesystem.Tests` (isolated) — 101 passed, 10 skipped (all
  symlink-capable or Linux-only tests, visibly skipped on this Windows machine), 0 failed.
- `dotnet test tests/bOps.Architecture.Tests` (isolated) — 14 passed, 0 skipped, 0 failed.
- Code review confirmed and fixed six findings: `fs.tail`'s non-strict UTF-8 decoding, Windows and
  Linux `fs.locks` folding a diagnostic-source failure or scan truncation into `complete:true`,
  Windows `fs.permissions` reading a directory's ACL through the file API and not flagging
  truncation past 200 entries, and `fs.mkdir`'s `fs.stat` verification accepting `exists:true`
  without confirming the path is a directory. See `HANDOFF.md` and
  [`docs/filesystem-troubleshooting.md`](../../docs/filesystem-troubleshooting.md) for detail.
- Linux-specific source paths (`fs.locks`/`fs.permissions` real `/proc` and `stat` evidence) are
  covered by new Linux-only tests in the suite but were verified only by code review here, not
  executed, since this machine is Windows; Windows/Linux CI is the authoritative platform evidence
  once the pull request is opened.
