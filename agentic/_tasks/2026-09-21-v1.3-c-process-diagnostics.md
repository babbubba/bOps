# V1.3-C — advanced process diagnostics

Status: **implemented (2026-09-22); pull request open, CI evidence recorded after it runs**
Recommended model effort: **medio**
Repository: `bOps`
Depends on: V1.3-B complete

## Outcome
Complete process troubleshooting without adding `process.start`, shell access or environment dumping. Preserve existing process tools; extend `process.inspect` additively and add `process.metrics`, `process.tree`, `process.modules`.

## Public contract
### process.inspect
Keep existing parameter `pid` and existing JSON fields. Add nullable: `parentPid`, `executablePath`, `commandLine`, `user`, `privateMemoryMb`, `virtualMemoryMb`, `handleOrFdCount`, `cpuTotalMs`, `ioReadBytes`, `ioWriteBytes`. Permission-denied fields become null; one unreadable field must not fail the observation. Never return environment variables.

### process.metrics
Parameters: `pid` required; `sampleMilliseconds` optional default 500, range 200–5000.
Output: `pid,exists,sampleMilliseconds,cpuPercent,workingSetMb,privateMemoryMb,virtualMemoryMb,threadCount,handleOrFdCount,readBytesPerSec,writeBytesPerSec,pageFaultsPerSec,partial`.
CPU is host-normalized 0–100. If process exits between samples return `exists:false,partial:true`.

### process.tree
Parameters: optional `rootPid`; `maxDepth` default 4/max 16; `limit` default 200/max 2000.
Rows: `pid,parentPid,name,user,depth`, deterministic parent/PID order plus `complete,truncated,skipped`.

### process.modules
Parameters: `pid`; optional `limit` default 200/max 1000; `maxOutputBytes` default 32768/max 131072.
Rows: `name,path,baseAddress,sizeBytes,version`, unique by normalized path, with completeness metadata.

## Implementation
Use existing System.Core for manifests/DTOs/formatting/base classes and System.Windows/Linux only for native collection. No new public Abstractions members expected.

### Windows
- `System.Diagnostics.Process`: memory, threads, TotalProcessorTime, modules.
- Add `System.Management` only to System.Windows; query `Win32_Process` by PID for ParentProcessId, ExecutablePath, CommandLine and GetOwner.
- P/Invoke `GetProcessIoCounters` and `GetProcessHandleCount`.
- CPU rate = delta TotalProcessorTime / wall interval / Environment.ProcessorCount.
- No PowerShell, WMIC executable, SeDebugPrivilege, arbitrary Process.Start.

### Linux
- `/proc/<pid>/stat`: parent PID + CPU counters.
- `/proc/<pid>/status`: UID, memory, threads.
- `/proc/<pid>/cmdline`: NUL-separated command line.
- `/proc/<pid>/exe`: executable.
- `/proc/<pid>/io`: cumulative I/O.
- count `/proc/<pid>/fd`.
- resolve UID from local `/etc/passwd` only.
- `/proc/<pid>/maps`: coalesced modules.
Treat ENOENT as exit race and EACCES as partial visibility.

## Validation
- [x] Extend System conformance for manifests and JSON shapes.
- [x] Real Windows/Linux tests inspect the test process and a child process.
- [x] Prove parent/child relation, non-negative metrics, bounds, cancellation, inaccessible/disappearing process behavior.
- [x] Architecture tests continue to reject `process.start`, shell/system.exec and process environment dump.

## Definition of Done
- [x] The agent can identify process ancestry, ownership, launch command, executable, resource rates and modules with bounded typed evidence.

## Evidence (2026-09-22)

- ADR: [ADR-0034](../../docs/architecture/adr/0034-advanced-process-diagnostics.md), Accepted. Decision D-030.
  User documentation: [`docs/process-diagnostics.md`](../../docs/process-diagnostics.md).
- Code, shared half (`bOps.Packages.System.Core`): `ProcessInspectResult` gains the ten nullable fields as `init` properties, so
  the existing positional shape and every existing caller are untouched; `ProcessDiagnosticsLimits`,
  `ProcessDiagnosticsArguments` (reject, never clamp — D-028), `ProcessDiagnosticsResults` (`ProcessSample`,
  `ProcessMetricsResult`, `ProcessTreeEntry`/`Snapshot`/`Row`/`Selection`, `ProcessModuleEntry`/`Snapshot`),
  `ProcessTreeBuilder` (depth-first, children by PID, depth and row bounds, visited set against a PID-reuse cycle),
  `ProcessDiagnosticsFormatting` (the three JSON shapes, module dedupe by normalized path, UTF-8 byte trimming),
  three manifests in `SystemToolManifests`, and three tool shells in `SystemToolBases` — `ProcessMetricsToolBase` owns the
  two-sample interval, the host-normalized CPU arithmetic and the `partial` decision, so neither OS package restates them.
- Code, Windows: `WindowsProcessInformation` (`Win32_Process` through `System.Management`, added to that package only; PID is the
  only value interpolated into a WQL query and it is already validated as an integer; `GetOwner` invoked on a path-bound object),
  `WindowsProcessCounters` (per-counter degradation to `null`), three kernel32 imports (`GetProcessIoCounters`,
  `GetProcessHandleCount`, `K32GetProcessMemoryInfo`), and `WindowsProcessMetricsTool`/`TreeTool`/`ModulesTool`.
- Code, Linux: `LinuxProcReader` (`stat`, `status`, `cmdline`, `exe`, `io`, `fd`, `maps`; ENOENT and EACCES both answer `null` and
  are told apart by whether the process directory still exists), `LinuxUserNames` (local `/etc/passwd` only, no NSS/LDAP round
  trip), and `LinuxProcessMetricsTool`/`TreeTool`/`ModulesTool`.
- Both providers register the three new tools; the provider tool-set tests now assert sixteen names in order on each platform.
- Tests, never mocking the operating system: 74 in `bOps.Packages.System.Windows.Tests` (up from 57), all against the real
  Windows host — the test process itself, a real `ping.exe` child this test spawns (parent/child proven through both
  `process.tree` and `process.inspect`'s `parentPid`), and PID 4, the protected System process, for the permission-denied path.
  155 in `bOps.Packages.System.Core.Tests` (up from 115) for the manifests, the argument bounds at their exact edges, the tree
  walk (order, depth cut, row cut, missing root, orphan, cycle), the rate arithmetic (exit mid-sample, CPU normalization,
  non-negative rates, the `partial` rule) and the JSON shapes including byte trimming. 129 in
  `bOps.Packages.System.Linux.Tests`, of which 8 are new and platform-independent: the `/proc` parsers are exercised against real
  captured kernel text on any host, including a process name containing spaces and parentheses, a kernel without `RssAnon`, and
  `maps` coalescing. The Linux tools themselves add 19 `[LinuxOnlyFact]`s that mirror the Windows ones.
- Architecture: `CoreNamesNoPackageTests` now also rejects every `process.*` and the inventory/event tool names in the core
  assemblies. New `NoGenericExecutionToolTests` scans the whole of `src/` for any tool literal in the
  `shell.*`/`system.exec`/`powershell.*`/`process.start`/`process.env*` families and for `/environ`, `ReadProcessMemory` and
  `GetEnvironmentVariables(` — the three ways a process environment gets dumped. It also asserts that the scan actually reached
  the source tree and would match a real manifest line, so it cannot pass by scanning nothing.
- Environment variables: the conformance suite sets a marked variable in the test process and then asserts that no output of any
  of the four tools contains its name or its value, on both platforms.

## Verified on this machine (Windows 11, 2026-09-22)

- `dotnet build bOps.slnx --configuration Release` — 0 warnings, 0 errors.
- `dotnet test bOps.slnx --configuration Release --no-build --filter "Category!=LiveModel"` — 2044 passed, 77 skipped, 1 failed.
  Per assembly (passed/skipped): System.Core 155/0, System.Windows 74/0, System.Linux 74/55, Architecture 14/0, Runtime 790/0,
  Policy 235/0, Docker 259/9, Api 110/0, Filesystem 66/5, Cli 74/0, PluginHost 49/0, Web 61/0, Network 10/0, Audit 10/0,
  Memory 23/0, Providers 26/0, Service.Windows 7/5, Service.Linux 7/3.
- The one failure is `bOps.Api.Tests.AgentsAndApprovalsEndpointsTests.StartTask_RejectsWorkBeyondTheConfiguredConcurrencyLimit`,
  a load-sensitive concurrency-limit test that passed in the full run immediately before this batch's own tests were added and
  passes on its own re-run. It exercises no System package and touches nothing this batch changed. Recorded here rather than
  re-run until green and called clean.
- Skipped tests are platform-gated and were **not** verified on this machine: the Linux System and Service tools (55 + 3), the
  Windows service tests that need elevation (5), Docker tests needing a daemon (9) and Filesystem tests needing Linux or symlink
  privilege (5). They belong to the CI gate.

## What could not be verified here, and why

- **Every Linux tool path that touches a real `/proc`.** This is a Windows development machine, so the 55 `[LinuxOnlyFact]`s
  skip visibly. What is covered here instead is every parser they depend on, against captured real kernel output, as ordinary
  platform-independent facts; the tools themselves are verified by the `ubuntu-latest` CI job.
- **A privileged Windows run.** The tests ran as an ordinary user, so the permission-denied path was exercised (PID 4) but the
  privileged path — where `Win32_Process` returns `CommandLine` for other users' processes — was not. The assertions are written
  to accept both outcomes rather than to pin the one this machine happens to produce.
- **A `hidepid=2` Linux mount**, where other users' processes are invisible entirely and land in `process.tree`'s `skipped`. The
  counting path is unit-tested; the mount is not something CI provides.

## Residual and known limitations

- `process.tree` makes one WMI query plus one `GetOwner` per returned row on Windows. At the default limit of 200 rows that is
  comfortably inside the 30-second tool timeout on the machines tested; at `limit: 2000` on a very busy Windows host it may not
  be, and the operator raises the timeout rather than the tool silently returning less.
- `privateMemoryMb` is `null` on Linux kernels older than 4.5, which do not report `RssAnon`. Reconstructing it from
  `VmRSS - RssFile - RssShmem` was not done, because those fields arrived in the same kernel release.
- Module paths are deduplicated case-insensitively. That is correct on Windows and theoretically over-eager on Linux, where a
  process could map `/lib/Foo.so` and `/lib/foo.so` at once; no real system does this.
- `version` is always `null` for a Linux module. The kernel records none, and deriving one from a symlinked `.so` file name
  would be a fact it never stated.
- Clock ticks per second is taken as the constant 100 rather than read through `sysconf(_SC_CLK_TCK)`, which would need another
  P/Invoke for a value that is 100 on every architecture this project targets.

## CI evidence

Recorded below once the pull request's workflow completes for its head commit.
