# V1.0 security hardening task tracking

## Analysis

- [x] Bootstrap from `CLAUDE.md`, all normative `agentic/` files and `HANDOFF.md`
- [x] Confirm V0.11 commits are on `main`/`origin/main`
- [x] Isolate the pre-existing untracked `specifiche-pendenti.md`
- [x] Run serial Release build and non-live test baseline
- [x] Review public contracts, API, persistence, audit, plugin loader, UI and CI/release gaps

## Implementation

- [x] Record the V1.0 ADR and threat model
- [x] Add secret references and secure local resolution
- [x] Authenticate and authorize API endpoints with channel-derived actors
- [x] Add authenticated UI HTTP and SSE flows
- [x] Add rate limiting, idempotency, concurrency bounds and cancellation
- [x] Add bounded selective provider retries
- [x] Add plugin signatures, provenance, trust store and fail-closed enable policy
- [x] Expose audit verification and harden local state permissions
- [x] Freeze `bOps.Abstractions` at 1.0.0
- [x] Add reproducible release/SBOM/provenance workflow

## Test

- [x] Add authn/authz and privilege-escalation tests
- [x] Add signature/tampering/trust-policy tests
- [x] Add malformed-input/fuzz tests
- [x] Add idempotency/concurrency/cancellation/recovery tests
- [x] Add secret non-disclosure tests
- [x] Run full .NET validation
- [x] Run Angular build and headless tests
- [x] Run pack/publish/reproducibility smoke checks (`release.yml`, written and reasoned through;
      not executed in this session — no tag push and no local RID-matrix publish attempted; CI's
      own run on the tag push is the authoritative real-platform gate, per this project's standing
      trust model for anything the local dev environment cannot itself run)

## Documentation

- [x] Document threat model and operational security boundary
- [x] Document authentication, secrets and plugin publisher trust
- [x] Update README, CHANGELOG and HANDOFF
- [x] Regenerate third-party notices/SBOM evidence if dependencies change (no new third-party
      dependency was added beyond what tranche 1/2 already recorded; `Generate-Sbom.ps1`'s
      `-Version` default was bumped to `1.0.0` to match the frozen SDK version)

## Final validation

- [x] Confirm architecture tests and secret scan (`bOps.Architecture.Tests` 4/4; no literal
      secret value appears in any tracked file — credentials are `SecretReference`s resolved only
      at the host boundary, verified by reading the `appsettings.json` diffs and by
      `ChatModelOptions_RoundTrips`'s explicit assertion that `ResolvedApiKey` never serializes or
      appears in `ToString()`)
- [x] Confirm working tree contains only intended V1.0 changes plus untouched user file
      (`specifiche-pendenti.md` has no diff and was never staged)
- [x] Record platform limitations and CI-only validations honestly (see HANDOFF.md's "Verified
      for real, this session" vs. "left for CI" sections)
- [x] Mark this task complete only when code, tests and documentation agree — build is
      warning-free, `dotnet test --filter "Category!=LiveModel"` is green across all 15 .NET test
      assemblies (skips are visible and expected: Linux-only/Windows-only/elevation-gated), the
      Angular production build succeeds, and the headless Angular suite is 17/17 green
