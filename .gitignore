bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
publish/
TestResults/
# SBOMs are generated from each final build/release artifact and uploaded by CI; snapshots are
# deliberately not committed because a checked-in SBOM becomes stale as soon as restore changes.
sbom/
*.user
*.suo
*.userprefs
.vs/
.vscode/
.idea/
*.received.*

# Local configuration and secrets — never commit
appsettings.Development.json
appsettings.Local.json
*.env
.env
secrets.json

# Runtime state (root-anchored: docs/plugins/ is documentation, not the install directory)
/plugins/
*.db
*.db-shm
*.db-wal
audit*.jsonl

# Node (web/bops-ui, Phase 2)
node_modules/
dist/
.angular/

# Claude Code local session config — personal, never shared
.claude/settings.local.json
